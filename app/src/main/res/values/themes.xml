<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.MyBest" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/app_window_background</item>
    </style>

    <style name="Theme.MyBest.Starting" parent="Theme.SplashScreen.IconBackground">
        <item name="windowSplashScreenBackground">@color/splash_screen_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_progress_foreground</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/ic_progress_background</item>
        <item name="postSplashScreenTheme">@style/Theme.MyBest</item>
    </style>
</resources>
